# bJSON project .gitignore file

# ignore the build directory:
/build

# ignore the .vs folder inside the premake directory (root and testing), as well as the solution that is generated by the batch file AND the premake executable itself (as well as all other executables):
/premake/.vs
*.sln
*.exe

# ignore the tests header (if it's even there) since we sourced that from its own repository
tests/include

# ADD files and folders as needed